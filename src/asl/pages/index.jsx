import Head from 'next/head'
import Image from 'next/image'
import { Inter } from '@next/font/google'

const inter = Inter({ subsets: ['latin'] })
const Home=()=>{
  return(
   <div>
   Hello thsisa dkasdjfk 
   </div>
  );
}

export default Home